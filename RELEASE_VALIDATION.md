# Release validation

This branch exists only to run the consolidated production UI through Netlify Deploy Preview before final handoff. It does not change application behavior.

Validation target: the exact `main` application code containing the static CUSTOM control, secure custom-roll graphic/text, rigid-body trust badge, and click/tap tray-to-roll hint.
